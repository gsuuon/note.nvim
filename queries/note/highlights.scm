(item
  (marker
    (marker_task
      ((marker_task_pending) @note.pending.marker)))
  ((content) @note.pending.content))

(item
  (marker
    (marker_task
      ((marker_task_paused) @note.paused.marker)))
  ((content) @note.paused.content))

(item
  (marker
    (marker_task
      ((marker_task_done) @note.done.marker)))
  ((content) @note.done.content))

(item
  (marker
    (marker_task
      ((marker_task_cancelled) @note.cancelled.marker)))
  ((content) @note.cancelled.content))

(item
  (marker
    (marker_task
      ((marker_task_current) @note.current.marker)))
  ((content) @note.current.content))

(item
  (marker
    (marker_property
      ((marker_property_info) @note.info.marker)))
  ((content) @note.info.content))

(item
  (marker
    (marker_property
      ((marker_property_label) @note.label.marker)))
  ((content) @note.label.content ))

(code_block) @markup.raw.block

(section_header) @markup.heading.1

(section
  (section_children
    (section
      ((section_header) @markup.heading.2))))


(section
  (section_children
    (section
      (section_children
        (section
          ((section_header) @markup.heading.3))))))


(section
  (section_children
    (section
      (section_children
        (section
          (section_children
            (section
              ((section_header) @markup.heading.4))))))))


